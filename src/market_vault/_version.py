"""MarketVault package version.

The installed wheel may not contain the repository root, so the runtime
version always comes from this module; pyproject.toml must stay consistent
with it (enforced by tests and scripts/check_release.py).
"""

__version__ = "0.4.0"
